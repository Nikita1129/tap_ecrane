# Required. The one shared password for /admin.
ADMIN_PASSWORD=change-me

# Optional. Signs the session cookie. If missing, it is derived from ADMIN_PASSWORD,
# which means changing the password logs every device out (usually what you want).
SESSION_SECRET=
